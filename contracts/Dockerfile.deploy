FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npx hardhat compile
# Runs once then exits; docker-compose restart: "no" ensures it doesn't loop
CMD ["sh", "-c", "npx hardhat run scripts/deploy.ts --network ganache || true"]
